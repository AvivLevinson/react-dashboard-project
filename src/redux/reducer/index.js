import * as actions from "../types";



export default function reducer(state, action) {
  switch (action.type) {
    case actions.GET_DATA_BY_DATE:
      return {  };

    case actions.SET_DATA_BY_DATE:
      return {};
    default:
 
      return state;
  }
}
